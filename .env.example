# ─── API URL ─────────────────────────────────────────────────────────────────
# Leave blank in dev (Vite proxy handles /api → localhost:5000)
# Set in production (deployed backend URL)
VITE_API_URL=

# Production example:
# VITE_API_URL=https://your-portfolio-api.onrender.com/api
